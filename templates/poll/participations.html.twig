{% extends 'layout.html.twig' %}

{% block rawbody %}
	{{ render(controller('App\\Controller\\ParticipationController::frame', {'poll': poll})) }}
	<script src="{{ asset('js/participations.js') }}"></script>
	<script>
		const pollUrl = '{{ poll.url }}';
	</script>
	<style>
		.participation-container {
			max-height: 100vh;
		}
	</style>
{% endblock %}
