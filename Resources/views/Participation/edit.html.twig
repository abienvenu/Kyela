{% extends "KyelaBundle::layout.html.twig" %}

{% block body %}
	<div class="jumbotron">
		<div class="container">
			<h1>{{ entity.participant.name }} - {{ entity.event.name }}</h1>
		</div>
	</div>

	<div class="container">
		<div class="pull-right">
			{{ form(delete_form) }}
		</div>
		{{ form(edit_form) }}
	</div>
{% endblock %}
