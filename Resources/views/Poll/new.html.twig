{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Poll creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('poll') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
