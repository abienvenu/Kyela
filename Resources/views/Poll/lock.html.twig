{% extends "KyelaBundle::layout.html.twig" %}

{% block body -%}
    <div class="jumbotron">
        <div class="container">
            <h1>{% trans %}lock{% endtrans %} - {{ poll.title }}</h1>
        </div>
    </div>

    <div class="container">
        <p>{% trans %}lock.explanation{% endtrans %}</p>
        <p>{% trans %}lock.explanation2{% endtrans %}</p>
        {{ form(form) }}
    </div>
{% endblock %}
